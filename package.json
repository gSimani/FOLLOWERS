{
  "name": "followers",
  "version": "1.0.0",
  "scripts": {
    "dev": "nodemon",
    "build": "echo 'No build step'",
    "deploy": "vercel --prod"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}